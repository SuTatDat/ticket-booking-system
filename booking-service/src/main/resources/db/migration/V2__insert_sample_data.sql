-- ================================================================
-- Flyway Migration Script V2
-- Description: Insert sample/initial data for booking service (OPTIONAL)
-- Author: System
-- Date: 2026-01-22
-- ================================================================

-- Note: This file is for demo/development purposes only
-- Remove or modify for production environment

-- You can add sample bookings here if needed for testing
-- Example:
-- INSERT INTO bookings (event_id, user_id, status, total_amount, created_at, updated_at, expires_at, version)
-- VALUES
--     (1, 1, 'CONFIRMED', 150.00, NOW(), NOW(), DATE_ADD(NOW(), INTERVAL 2 HOUR), 0),
--     (2, 2, 'PENDING', 200.00, NOW(), NOW(), DATE_ADD(NOW(), INTERVAL 1 HOUR), 0);

-- For now, this migration is intentionally left empty
-- Add your initial data here when needed
